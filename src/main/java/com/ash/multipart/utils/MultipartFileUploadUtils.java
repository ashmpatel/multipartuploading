package com.ash.multipart.utils;

/**
 * To hold the regex IF we wanted to check trawde data as it comes in.
 * Not used for now as this is NOT a requirement.
 */
public class MultipartFileUploadUtils {
    public static final String REGEX_RULES = "^01\\d{9}$|^1\\d{9}|^d{0}$";
}
